# ResearchReport-MA 详细开发计划

## 1. 项目目标与交付范围

本项目目标是开发一个面向科研报告协同写作的多 Agent 系统。系统输入为用户给出的研究主题，输出为结构完整、逻辑清晰、带有证据支撑和审稿反馈记录的 Markdown 科研报告。

第一阶段优先完成一个可运行的最小闭环版本：

- 用户输入研究主题
- Planner 拆解主题并生成报告大纲
- Researcher 根据子问题收集资料和证据
- Writer 根据大纲与证据生成初稿
- Reviewer 审查初稿并给出通过或修改意见
- 若通过，则导出最终报告
- 若不通过，则根据审稿意见进入一轮或多轮修改

后续阶段再逐步增强检索质量、引用格式、审稿标准、人工干预能力和可视化界面。

## 2. 技术选型与总体架构

系统以 CrewAI 为主要多 Agent 框架，使用 CrewAI Agent 表达不同角色，使用 CrewAI Flow 管理工作流、共享状态和条件分支。

核心技术组件如下：

- `CrewAI`：负责 Agent 定义、任务分配和协同执行
- `CrewAI Flow`：负责整体流程编排、状态传递和审稿循环
- `LLM Backend`：负责规划、推理、写作和审稿
- `Search / Retrieval Tools`：负责资料检索、网页搜索或本地知识检索
- `Markdown`：作为草稿、审稿记录和最终报告的统一输出格式
- `.env` 配置：管理模型 API Key、检索工具 Key 和运行参数

整体架构分为四层：

- 入口层：接收用户主题，启动流程
- 编排层：定义 Flow 状态、步骤和条件跳转
- Agent 层：定义 Planner、Researcher、Writer、Reviewer
- 工具与输出层：封装检索工具、文件输出、日志和报告保存

## 3. 仓库结构规划

按照 README 中的结构建立项目骨架：

```text
ResearchReport-MA/
├── README.md
├── PLAN.md
├── requirements.txt
├── .env.example
├── src/
│   ├── main.py
│   ├── flow.py
│   ├── agents.py
│   ├── tasks.py
│   ├── tools.py
│   └── config.py
├── outputs/
│   ├── drafts/
│   ├── reviews/
│   └── final_reports/
└── docs/
    └── demo_notes.md
```

各文件职责如下：

- `src/main.py`：命令行入口，读取用户主题并启动完整流程
- `src/flow.py`：定义科研报告生成 Flow，管理共享状态和循环逻辑
- `src/agents.py`：集中创建四类 Agent
- `src/tasks.py`：集中定义各 Agent 对应的任务模板
- `src/tools.py`：封装搜索、资料整理、文件保存等工具
- `src/config.py`：读取环境变量、模型参数和输出路径配置
- `outputs/drafts/`：保存 Writer 生成的报告草稿
- `outputs/reviews/`：保存 Reviewer 的审稿意见
- `outputs/final_reports/`：保存最终通过的报告
- `docs/demo_notes.md`：记录演示主题、运行结果和实验现象

## 4. 第一阶段：环境与项目初始化

第一阶段目标是让项目具备可安装、可运行、可配置的基础环境。

实现步骤：

1. 创建 `requirements.txt`
   - 添加 `crewai`
   - 添加 LLM 供应商 SDK 或 CrewAI 所需依赖
   - 添加环境变量读取工具，例如 `python-dotenv`
   - 如需联网搜索，添加对应搜索工具依赖

2. 创建 `.env.example`
   - 提供 API Key 示例字段
   - 提供模型名称字段
   - 提供搜索工具配置字段
   - 提供最大审稿轮数字段

3. 创建基础目录
   - `src/`
   - `outputs/drafts/`
   - `outputs/reviews/`
   - `outputs/final_reports/`
   - `docs/`

4. 实现 `src/config.py`
   - 从 `.env` 读取模型配置
   - 读取输出目录配置
   - 读取最大迭代轮数
   - 为其他模块提供统一配置对象

完成标准：

- 项目依赖可安装
- 配置文件字段清晰
- 目录结构完整
- 后续模块可以通过 `config.py` 获取配置

## 5. 第二阶段：定义共享状态模型

多 Agent 协作的关键是共享状态。需要先明确 Flow 中各步骤传递的数据结构。

计划在 `src/flow.py` 中定义一个报告生成状态，至少包含：

- `topic`：用户输入的研究主题
- `outline`：Planner 生成的大纲
- `sub_questions`：拆解后的研究子问题
- `task_list`：任务顺序和成功标准
- `evidence_notes`：Researcher 整理后的证据笔记
- `draft_report`：Writer 生成的 Markdown 草稿
- `review_result`：Reviewer 的审稿结论
- `revision_suggestions`：Reviewer 的修改建议
- `iteration`：当前修改轮数
- `approved`：是否通过审稿
- `final_report_path`：最终报告保存路径

实现重点：

- 状态字段命名要稳定，方便各 Agent 引用
- 每一步只修改自己负责的状态字段
- 审稿反馈需要能回流到 Planner、Researcher 或 Writer
- 需要记录迭代次数，避免无限循环

完成标准：

- Flow 可以初始化状态
- 每个步骤都能读取上一步输出
- 审稿循环可以根据 `approved` 和 `iteration` 判断是否继续

## 6. 第三阶段：实现四类 Agent

Agent 定义集中放在 `src/agents.py` 中。每个 Agent 需要包括角色、目标、背景说明和行为约束。

### 6.1 Planner Agent

职责：

- 理解用户主题
- 判断主题边界和研究重点
- 拆解为若干子问题
- 生成报告大纲
- 定义每个部分的写作目标和成功标准

实现计划：

- 设置 Planner 的 role 为科研报告规划者
- goal 聚焦于任务拆解和结构设计
- backstory 强调学术写作、问题分解和研究路线设计能力
- 输出格式固定为结构化 Markdown 或 JSON 风格文本

输出内容：

- 报告标题建议
- 一级和二级大纲
- 子问题列表
- 每个子问题对应的证据需求
- 写作要求和评价标准

### 6.2 Researcher Agent

职责：

- 根据 Planner 的子问题检索资料
- 摘要化整理证据
- 标注来源、关键词和可用性
- 避免使用无来源或低可信度信息

实现计划：

- 设置 Researcher 的 role 为科研资料研究员
- 接入 `tools.py` 中的搜索工具
- 对每个子问题生成独立资料笔记
- 对资料进行可信度和相关性判断

输出内容：

- 证据列表
- 来源摘要
- 关键词
- 与大纲章节的对应关系
- 需要进一步确认的信息

### 6.3 Writer Agent

职责：

- 根据大纲和证据写作报告初稿
- 保持章节结构稳定
- 尽量避免无证据断言
- 将证据融入论述，而不是简单堆砌资料

实现计划：

- 设置 Writer 的 role 为科研报告撰写者
- 输入 Planner 的大纲和 Researcher 的证据笔记
- 输出完整 Markdown 报告
- 使用统一报告结构，例如摘要、引言、主体分析、结论、参考资料

输出内容：

- Markdown 格式报告草稿
- 清晰章节标题
- 引用或来源说明
- 对证据不足部分的谨慎表述

### 6.4 Reviewer Agent

职责：

- 检查报告是否符合大纲
- 检查论证逻辑是否完整
- 识别 unsupported claims
- 检查引用和证据是否足够
- 决定通过或要求修改

实现计划：

- 设置 Reviewer 的 role 为科研报告审稿人
- 定义明确审稿维度
- 输出审稿结论和修改建议
- 将问题分类为结构问题、证据问题、逻辑问题和表达问题

输出内容：

- `APPROVED` 或 `REJECTED`
- 总体评价
- 按章节列出的修改建议
- 是否需要重新检索资料
- 是否需要调整大纲

完成标准：

- 四个 Agent 都能被独立创建
- 每个 Agent 的输入输出边界清晰
- Agent 提示词中包含稳定的输出格式要求

## 7. 第四阶段：定义任务模板

任务定义集中放在 `src/tasks.py` 中，避免把长提示词散落在 Flow 逻辑里。

计划实现以下任务：

1. `create_planning_task`
   - 输入：用户主题
   - 执行者：Planner
   - 输出：大纲、子问题、任务列表和写作标准

2. `create_research_task`
   - 输入：主题、子问题、证据需求
   - 执行者：Researcher
   - 输出：结构化证据笔记

3. `create_writing_task`
   - 输入：大纲、写作要求、证据笔记
   - 执行者：Writer
   - 输出：Markdown 初稿

4. `create_review_task`
   - 输入：大纲、证据笔记、报告草稿
   - 执行者：Reviewer
   - 输出：审稿结论和修改建议

5. `create_revision_task`
   - 输入：原草稿、审稿意见、证据笔记
   - 执行者：Writer，必要时可重新调用 Planner 或 Researcher
   - 输出：修订后的报告草稿

完成标准：

- 每个任务都有明确 description
- 每个任务都有 expected_output
- 任务之间的数据依赖清晰
- 修改任务可以复用前一轮审稿结果

## 8. 第五阶段：实现工具模块

工具模块放在 `src/tools.py`，第一阶段以简单、稳定为主，不追求复杂检索系统。

计划工具：

1. 搜索工具
   - 根据关键词或子问题进行网页搜索
   - 返回标题、链接、摘要和来源信息
   - 后续可替换为学术搜索、arXiv、Semantic Scholar 或本地 RAG

2. 资料整理工具
   - 将搜索结果压缩为结构化笔记
   - 记录来源链接
   - 标注相关章节

3. 文件保存工具
   - 保存草稿到 `outputs/drafts/`
   - 保存审稿意见到 `outputs/reviews/`
   - 保存最终报告到 `outputs/final_reports/`
   - 文件名包含主题关键词和时间戳

4. Markdown 清理工具
   - 确保报告标题、章节层级和参考资料格式基本一致
   - 去除明显的格式噪声

完成标准：

- 工具函数可以被 Flow 或 Agent 调用
- 输出结果格式稳定
- 文件保存路径可配置
- 没有搜索 API 时也能用 mock 数据跑通流程

## 9. 第六阶段：实现 Flow 编排逻辑

`src/flow.py` 是系统核心，需要把四个 Agent 组织成完整协作流程。

基础流程：

1. 接收用户主题并初始化状态
2. 调用 Planner 生成大纲和任务列表
3. 调用 Researcher 收集证据
4. 调用 Writer 生成初稿
5. 保存初稿
6. 调用 Reviewer 审稿
7. 保存审稿意见
8. 判断是否通过
9. 若通过，保存最终报告
10. 若不通过，进入修订循环

审稿循环计划：

- 如果 Reviewer 指出结构问题，则让 Planner 重新调整大纲
- 如果 Reviewer 指出证据不足，则让 Researcher 补充资料
- 如果 Reviewer 主要指出表达和逻辑问题，则直接让 Writer 修订
- 每轮修订后重新交给 Reviewer
- 达到最大轮数后，即使未完全通过，也输出当前最佳版本，并保留审稿意见

完成标准：

- 单次流程可以完整运行
- 审稿通过时正确保存最终报告
- 审稿不通过时可以进入至少一轮修订
- 最大轮数限制有效
- 每轮输出都有文件记录

## 10. 第七阶段：实现命令行入口

`src/main.py` 负责让用户可以直接运行系统。

计划支持两种输入方式：

- 命令行参数输入研究主题
- 如果没有参数，则在终端中提示用户输入

示例运行方式：

```bash
python src/main.py "大语言模型在科研写作中的应用与风险"
```

入口逻辑：

1. 加载配置
2. 获取研究主题
3. 初始化 Flow
4. 执行完整流程
5. 在终端打印关键进度
6. 输出最终报告路径

完成标准：

- 用户可以通过一条命令启动项目
- 运行过程中能看到当前步骤
- 运行结束后能找到最终输出文件

## 11. 第八阶段：输出格式设计

最终报告采用 Markdown 格式，建议结构如下：

```markdown
# 报告标题

## 摘要

## 1. 引言

## 2. 研究背景

## 3. 核心问题分析

## 4. 证据与讨论

## 5. 风险、局限与未来方向

## 6. 结论

## 参考资料
```

审稿意见采用 Markdown 格式，建议结构如下：

```markdown
# 审稿意见

## 审稿结论

## 总体评价

## 主要问题

## 分章节修改建议

## 是否需要补充检索

## 是否通过
```

完成标准：

- 草稿、审稿意见和最终报告格式统一
- 文件可直接阅读
- 后续可以方便转换为 PDF 或 Word

## 12. 第九阶段：测试与验证

第一阶段测试以流程验证为主，重点检查系统能否稳定完成端到端任务。

测试主题建议：

- 大语言模型在科研写作中的应用与风险
- 多智能体系统在教育场景中的协同机制
- 检索增强生成技术对知识问答系统的影响

验证内容：

- Planner 是否能生成合理大纲
- Researcher 是否能围绕子问题整理证据
- Writer 是否遵循大纲写作
- Reviewer 是否能指出具体问题
- 审稿循环是否能触发
- 文件是否正确保存

完成标准：

- 至少完成 3 个主题的端到端测试
- 每个主题都有草稿、审稿记录和最终报告
- 记录失败案例和改进方向到 `docs/demo_notes.md`

## 13. 第十阶段：增强方向

完成最小闭环版本后，可以按优先级逐步增强：

1. 引用与来源增强
   - 增加标准引用格式
   - 对每条关键论断绑定来源
   - 检查引用是否真实存在

2. 检索能力增强
   - 接入学术搜索 API
   - 支持 PDF 文献解析
   - 支持本地文献库检索

3. 审稿能力增强
   - 增加评分量表
   - 支持多个 Reviewer
   - 引入事实核查 Agent

4. 协作机制增强
   - 让 Planner 根据 Reviewer 反馈动态重排任务
   - 让 Researcher 与 Writer 多轮交互
   - 增加共享记忆或项目级知识库

5. 用户体验增强
   - 增加 Web UI
   - 支持中途人工修改大纲
   - 支持选择报告长度和写作风格

6. 输出增强
   - Markdown 转 PDF
   - Markdown 转 Word
   - 自动生成摘要页和参考文献页

## 14. 里程碑安排

建议按以下里程碑推进：

### Milestone 1：基础骨架

目标：项目可以安装依赖并启动。

交付内容：

- `requirements.txt`
- `.env.example`
- `src/config.py`
- 基础目录结构

### Milestone 2：单轮多 Agent 流程

目标：实现 Planner、Researcher、Writer、Reviewer 的顺序执行。

交付内容：

- `src/agents.py`
- `src/tasks.py`
- `src/flow.py` 单轮流程
- `src/main.py`

### Milestone 3：审稿与修订循环

目标：Reviewer 可以决定是否退回修改，并触发新一轮写作。

交付内容：

- 修订任务
- 最大迭代轮数
- 审稿意见保存
- 修订草稿保存

### Milestone 4：检索与引用增强

目标：Researcher 能提供更可信的资料来源。

交付内容：

- 搜索工具
- 来源摘要
- 引用格式
- 证据与章节映射

### Milestone 5：实验记录与演示

目标：形成可展示的实验结果。

交付内容：

- 3 个示例主题输出
- `docs/demo_notes.md`
- 运行说明
- 已知问题与未来改进

## 15. 开发优先级

优先级从高到低如下：

1. 跑通端到端流程
2. 保证 Agent 输出格式稳定
3. 实现审稿循环
4. 保存所有中间产物
5. 提升资料检索质量
6. 优化报告质量
7. 增强界面和导出能力

开发时应先确保系统能完整运行，再逐步提高写作质量和检索可信度。

## 16. 风险与应对策略

### 风险 1：Agent 输出格式不稳定

应对策略：

- 在任务提示词中明确输出格式
- 使用结构化字段
- 对关键输出做简单解析和校验

### 风险 2：检索结果质量不足

应对策略：

- 第一阶段允许 mock 数据跑通流程
- 后续接入更可靠的学术检索源
- Reviewer 专门检查证据不足问题

### 风险 3：审稿循环无限进行

应对策略：

- 设置最大迭代轮数
- 每轮记录修改原因
- 达到上限后输出当前最佳版本

### 风险 4：多个 Agent 职责重叠

应对策略：

- 在 Agent prompt 中明确职责边界
- Planner 只负责结构，Researcher 只负责资料，Writer 只负责成稿，Reviewer 只负责评价
- 通过共享状态传递结果，而不是让每个 Agent 重做全部工作

### 风险 5：最终报告缺少真实引用

应对策略：

- Researcher 输出必须包含来源字段
- Writer 只能优先使用已整理证据
- Reviewer 检查无来源断言

## 17. 最小可行版本定义

MVP 版本需要满足以下条件：

- 可以通过命令行输入研究主题
- 系统自动完成规划、检索、写作、审稿和导出
- 至少包含四个独立 Agent
- 使用共享状态串联各 Agent 输出
- 支持至少一轮审稿修改
- 输出 Markdown 草稿、审稿意见和最终报告
- 运行说明清晰，能够被他人复现

完成 MVP 后，再进入引用增强、检索增强和用户界面开发。
